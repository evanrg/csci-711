use image::{ImageBuffer, RgbImage};

mod geometry;
mod lighting;
mod world;

fn main() {
    // let w = 100;
    // let h = 100;

    // let mut img: RgbImage = ImageBuffer::new(w, h);

    // // Checkerboard
    // // +y is downward
    // for x in 0..w {
    //     for y in 0..h {
    //         let red = if (x + y) % 2 == 0 { 255 } else { 0 };

    //         *img.get_pixel_mut(x, y) = image::Rgb([red, 0, 0]);
    //     }
    // }

    // img.save("red.png").unwrap();
}
